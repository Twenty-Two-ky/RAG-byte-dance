# 变更记录 001 — RAG 层升级：混合检索 + Re-ranking

- **日期**：2026-03-13
- **阶段**：Phase 1
- **涉及文件**：`core/rag_engine.py`、`requirements.txt`

---

## 一、变更背景

原始 RAG 检索仅使用 ChromaDB 向量相似度检索，存在以下问题：

1. 纯语义检索对关键词精确匹配弱（如专有名词、产品名）
2. 自写 `TextSplitter` 类约 80 行，逻辑冗余且未经工业验证
3. 检索结果无精排，Top-K 顺序不一定最优
4. 异常处理使用裸 `except:`，错误信息丢失

---

## 二、具体变更

### 2.1 `requirements.txt` — 新增依赖

```
langchain-text-splitters==0.3.0   # LangChain 文本分割器
rank-bm25==0.2.2                  # BM25 关键词检索
sentence-transformers==3.3.1      # BGE-Reranker 精排模型
numpy>=1.24.0                     # RRF 融合计算
```

---

### 2.2 `core/rag_engine.py` — 新增导入

```python
import logging
from langchain_text_splitters import RecursiveCharacterTextSplitter
from rank_bm25 import BM25Okapi
from sentence_transformers import CrossEncoder
import numpy as np

logger = logging.getLogger(__name__)
```

---

### 2.3 删除 `TextSplitter` 类（约 80 行）

**原因**：自写递归分割逻辑被 LangChain 工业级实现替代，功能完全覆盖且更稳定。

---

### 2.4 `__init__` 新增初始化

```python
# LangChain 文本分割器（替换自写 TextSplitter）
self.text_splitter = RecursiveCharacterTextSplitter(
    chunk_size=500,
    chunk_overlap=100,
    separators=["\n\n", "\n", "。", "！", "？", ".", "!", "?", " ", ""]
)

# BM25 索引（关键词检索）
self._bm25_index = None
self._bm25_corpus = []
self._bm25_doc_ids = []
self._build_bm25_index()

# BGE-Reranker 精排模型（加载失败时优雅降级）
try:
    self.reranker = CrossEncoder('BAAI/bge-reranker-base')
except Exception as e:
    logger.warning(f"BGE-Reranker 加载失败，将跳过重排序: {e}")
    self.reranker = None
```

---

### 2.5 新增私有方法

#### `_build_bm25_index()`
从 ChromaDB 读取所有已存储的文本块，构建 BM25 倒排索引。每次 `add_knowledge()` 后自动重建。

#### `_bm25_search(query, top_k)`
对查询进行分词，在 BM25 索引中打分并返回 Top-K 文本块 ID 及得分。

#### `_rerank(query, candidates)`
用 BGE-Reranker（Cross-Encoder）对候选列表重新打分排序，返回精排后结果。

---

### 2.6 新增公共方法 `hybrid_search(query, top_k)`

三阶段检索流水线：

```
用户问题
   ↓
向量检索 Top-10 (ChromaDB cosine similarity)
   +
BM25 关键词检索 Top-10
   ↓
RRF 融合（向量权重 0.7，BM25 权重 0.3）
   ↓
BGE-Reranker 精排
   ↓
返回 Top-K 结果
```

**RRF 公式**：
```
score(d) = Σ weight_i / (rank_i(d) + 60)
```

---

### 2.7 修改 `add_knowledge()`

```python
# 之前
chunks = TextSplitter.recursive_split(final_content, chunk_size=500, overlap=100)

# 之后
chunks = self.text_splitter.split_text(final_content)
# 添加完成后重建 BM25 索引
self._build_bm25_index()
```

---

### 2.8 修改 `search_context()`

内部调用改为 `hybrid_search()`，外部接口签名不变：

```python
def search_context(self, query: str, top_k: int = 5) -> str:
    try:
        return self.hybrid_search(query, top_k)
    except Exception as e:
        logger.warning(f"混合检索失败，回退纯向量检索: {e}")
        # 回退到原始向量检索逻辑
        ...
```

---

### 2.9 改进 `GeminiEmbeddingFunction` 错误处理

```python
# 之前
except:
    return [[0.0] * 768]

# 之后
except Exception as e:
    logger.error(f"Embedding 生成失败: {e}")
    return [[0.0] * 768]
```

---

## 三、影响范围

| 模块 | 是否受影响 | 说明 |
|------|-----------|------|
| `core/rag_engine.py` | 是 | 核心改动文件 |
| `requirements.txt` | 是 | 新增 4 个依赖 |
| `ui/main.py` | 否 | 接口签名未变，零改动 |
| 其他模块 | 否 | 无依赖关系 |

---

## 四、降级策略

| 场景 | 处理方式 |
|------|---------|
| BGE-Reranker 模型下载失败 | 跳过精排，直接返回 RRF 融合结果 |
| hybrid_search 整体失败 | 回退到纯向量检索 |
| BM25 索引为空 | 仅使用向量检索结果 |
